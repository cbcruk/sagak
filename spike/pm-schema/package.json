{
  "name": "spike-pm-schema",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "report": "vitest run --reporter=basic",
    "typecheck": "tsc --noEmit",
    "test:browser": "vitest run -c vitest.browser.config.ts",
    "capture": "vite tools"
  }
}
