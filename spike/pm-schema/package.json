{
  "name": "spike-pm-schema",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run -c vitest.browser.config.ts",
    "capture": "vite tools",
    "typecheck": "tsc --noEmit"
  }
}
